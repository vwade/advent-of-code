#include "libft.h"

int
get_next_line(int fd, char **line)
{
	
}

void
expense_report(char *input)
{
	int		fd = open(input, O_RDONLY);
	char	*line;
	int		*report =
	
	while (get_next_line(fd, &line) > 0)
	{
		
	}
}
